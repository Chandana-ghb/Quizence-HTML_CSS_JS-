{
  "name": "online-quiz-platform",
  "version": "1.0.0",
  "description": "Online Quiz Platform using JSON Server",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
